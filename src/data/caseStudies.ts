export interface CaseStudy {
  id: string;
  title: string;
  hash: string;
  category: string;
  severity: string;
  problem: string;
  rca: string;
  codeProof: string;
  takeaway: string;
}

export const caseStudies: CaseStudy[] = [
  {
    "id": "01",
    "title": "The Cross-Platform Path Crash",
    "hash": "ea50ed7d1c990cf70f9585c727aa9137e2fffa1c",
    "category": "Cross-Platform Compatibility / File I/O",
    "severity": "🔥 Critical (Application Hard Crash)",
    "problem": "During cross-platform testing on Windows 10, clicking the \"Generate PDF\" button resulted in an immediate, silent hard crash of the Flutter engine. No error dialog was shown to the user, and all unsaved form data was instantly lost.",
    "rca": "By attaching a debugger to the Windows executable, I traced the exception to an `IOException`. The codebase contained a hardcoded file path specifically formatted for macOS (`/Users/emirnurkovic/Desktop/urnecitest`). \n\nWhen the Windows OS attempted to parse this Unix-style path, it failed to find a valid drive letter (`C:\\`), throwing an unhandled exception that crashed the app.",
    "codeProof": "As a QA and developer, I identified the bad practice of hardcoding paths and removed the static macOS string, delegating file path generation to the native OS via the `file_picker` package.\n\n```diff\n--- a/lib/screens/zastita_na_radu_screen.dart\n+++ b/lib/screens/zastita_na_radu_screen.dart\n@@ -2030,12 +2030,6 @@ class _ZastitaNaRaduScreenState extends State<ZastitaNaRaduScreen>\n \n       templateDoc.dispose();\n \n-      final String folderPath = '/Users/emirnurkovic/Desktop/urnecitest';\n-      final Directory outputDir = Directory(folderPath);\n-      if (!outputDir.existsSync()) {\n-        outputDir.createSync(recursive: true);\n-      }\n-\n       final String rawName = _brojZapisnikaController.text.trim();\n       final String safeName =\n           rawName.isEmpty ? 'znr' : rawName.replaceAll(RegExp(r'[^\\w\\-]+'), '_');\n```",
    "takeaway": "**Never trust environmental assumptions.** A core principle of QA is testing across the *entire* matrix of supported environments. A test passing on macOS does not guarantee safety on Windows. This bug drove me to implement strict cross-platform boundary checks in my test plans."
  },
  {
    "id": "02",
    "title": "The Render Deadlock",
    "hash": "1af9eff75a77405279a44eea44e4084f84cba701",
    "category": "State Management / Performance",
    "severity": "🛑 High (UI Freeze / Hang)",
    "problem": "Users reported that while filling out the \"Potvrde\" (Certificates) dialog box, typing into certain text fields or updating the date would completely freeze the application. The UI became completely unresponsive to clicks, forcing users to forcefully kill the application process via Task Manager.",
    "rca": "This was a classic Flutter state management bug. Text fields inside the dialog were triggering a global `setState(() {})` on every single keystroke (`onChanged`). Because the parent widget was massive, this caused an infinite render loop (or render deadlock), overwhelming the UI thread and freezing the app.",
    "codeProof": "I removed the improper `setState` triggers from the `onChanged` callbacks. State for the dialog was localized, preventing the massive parent tree from rebuilding 60 times a second while the user typed.\n\n```diff\n--- a/lib/screens/zastita_na_radu_screen.dart\n+++ b/lib/screens/zastita_na_radu_screen.dart\n@@ -730,7 +730,6 @@ class _ZastitaNaRaduScreenState extends State<ZastitaNaRaduScreen> {\n                                         contentPadding: EdgeInsets.symmetric(\n                                             vertical: 8, horizontal: 10),\n                                       ),\n-                                      onChanged: (_) => setState(() {}),\n                                     ),\n                                   ),\n                                   Padding(\n@@ -742,7 +741,6 @@ class _ZastitaNaRaduScreenState extends State<ZastitaNaRaduScreen> {\n                                         contentPadding: EdgeInsets.symmetric(\n                                             vertical: 8, horizontal: 10),\n                                       ),\n-                                      onChanged: (_) => setState(() {}),\n                                     ),\n                                   ),\n```",
    "takeaway": "**Performance testing is functional testing.** A UI freeze isn't just a \"slow app\"—it's a critical functional failure. This bug taught me to always test forms with rapid inputs and to monitor thread performance during exploratory UI testing."
  },
  {
    "id": "03",
    "title": "UI Boundary Failures (Table Wrapping)",
    "hash": "c7d93a3cafee4ac05251f57d8a0f9677337d52f8",
    "category": "Boundary Testing / UI Rendering",
    "severity": "🟡 Medium (Visual Degradation / Data Obfuscation)",
    "problem": "During QA boundary testing, I inserted a 150-character company name into the \"Zaštita na radu\" module. When the PDF report and the UI tables rendered, the text did not wrap. Instead, it bled horizontally straight through the adjacent columns, rendering the entire data table unreadable.",
    "rca": "The PDF rendering engine (Syncfusion) and the Flutter UI were programmed with fixed row heights (`const double rowHeight = 26;`). They lacked dynamic height calculations. When the text exceeded the bounding box width, the layout engine simply forced the text to overflow instead of wrapping it to a new line and expanding the cell height.",
    "codeProof": "I wrote a custom text-wrapping algorithm (`_wrapPdfText`) and implemented dynamic row height calculations based on the longest string in the row, ensuring boundaries are respected regardless of string length.\n\n```diff\n--- a/lib/screens/zastita_na_radu_screen.dart\n+++ b/lib/screens/zastita_na_radu_screen.dart\n@@ -1508,29 +1508,11 @@ class _ZastitaNaRaduScreenState extends State<ZastitaNaRaduScreen> {\n         const double valueColWidth = 280;\n         final double tableWidth = labelColWidth + valueColWidth;\n         final double tableLeft = (potvrdaSize.width - tableWidth) / 2;\n-        const double rowHeight = 26;\n+        const double minRowHeight = 26;\n+        const double cellPadX = 8;\n+        const double cellPadY = 6;\n+        final double tableLineHeight = bodyFont.height + 1;\n...\n+        for (int r = 0; r < labels.length; r++) {\n+          final int maxLines = labelLines[r].length > valueLines[r].length\n+              ? labelLines[r].length\n+              : valueLines[r].length;\n+          final double dynamicHeight = maxLines * tableLineHeight + cellPadY * 2;\n+          rowHeights.add(dynamicHeight < minRowHeight ? minRowHeight : dynamicHeight);\n+        }\n```",
    "takeaway": "**Boundary-Value Analysis is non-negotiable.** Developers often test with \"John Doe\" (8 characters). Users will input \"Udruženje građana za zaštitu i unapređenje okoline\" (52 characters). This case study highlights my proactive approach to testing edge cases on input fields."
  },
  {
    "id": "04",
    "title": "Headless CI/CD Pipeline Stalls",
    "hash": "dddd98dfe18c0781bb84d9e8eef05a3b6ee9853f",
    "category": "DevOps / CI Automation / Infrastructure",
    "severity": "🛑 High (Deployment Blocker)",
    "problem": "Our automated GitHub Actions workflow (`windows-build.yml`), which compiles and packages the `.msix` Windows release, suddenly started timing out and failing after 60 minutes. The build was stuck on the MSIX packaging step.",
    "rca": "By investigating the GitHub Actions runner logs, I discovered the pipeline was hanging at `flutter pub run msix:create`. \nThe underlying CLI tool for MSIX certification was prompting the terminal with: `\"Do you want to install the certificate? (Y/n)\"`. \nBecause a CI/CD runner is a \"headless\" (non-interactive) environment, there was no human to press 'Y' or 'N', causing the pipeline to wait indefinitely until the server timed out.",
    "codeProof": "I modified the PowerShell script executed by the runner to pipe an explicit `\"n\"` (No) response directly into the command using `echo n |`. This forced the CLI to bypass the interactive prompt, allowing the pipeline to succeed in 3 minutes instead of timing out at 60 minutes.\n\n```diff\n--- a/.github/workflows/windows-build.yml\n+++ b/.github/workflows/windows-build.yml\n@@ -54,7 +54,9 @@ jobs:\n           Export-Certificate -Cert $cert -FilePath msix-cert.cer\n \n       - name: Build MSIX package\n-        run: flutter pub run msix:create --certificate-path msix-cert.pfx --certificate-password \"${{ secrets.MSIX_CERT_PASSWORD }}\"\n+        shell: pwsh\n+        run: |\n+          echo n | flutter pub run msix:create --certificate-path msix-cert.pfx --certificate-password \"${{ secrets.MSIX_CERT_PASSWORD }}\"\n```",
    "takeaway": "**Automation Requires Headless Resiliency.** QA isn't just about testing the app; it's about testing the pipeline that delivers the app. This bug demonstrates my ability to debug infrastructure-as-code and ensure reliable, automated delivery systems."
  },
  {
    "id": "05",
    "title": "JSON State Deserialization",
    "hash": "2ed109fe27877d6254d251585c6c688744b65985",
    "category": "Data Persistence / Backwards Compatibility",
    "severity": "🟡 Medium (Intermittent Data Loss)",
    "problem": "The application includes an offline \"Draft Auto-save\" feature. Users reported that after saving a draft and reloading the application the next day, their lengthy remarks in the \"Primjedbe\" and \"Napomena\" fields were suddenly blank, resulting in lost work.",
    "rca": "Upon reviewing the JSON payload saved to the local device storage, I found a state deserialization mismatch. Over time, the variable names in the Flutter code had been updated (e.g., from `primjedbeINedostatci` to `pdf9PrimjedbeINedostatci`, and finally just `primjedbe`). \nHowever, older drafts were saved with the legacy keys. When the app attempted to read the JSON file, it looked for the *new* key, found `null`, and erased the user's data.",
    "codeProof": "I implemented a robust fallback mapping system in the JSON loader. The application now gracefully checks for modern keys, and if missing, safely falls back to legacy aliases to guarantee backwards compatibility.\n\n```diff\n--- a/lib/screens/pregledpdf.dart\n+++ b/lib/screens/pregledpdf.dart\n@@ -1928,6 +1933,17 @@ class _PregledPdfPageState extends State<PregledPdfPage> {\n                 sectionData['gromobranska'] ??\n                     sectionData['gromobr anska'] ??\n                     '';\n+            section.primjedbeController.text =\n+                sectionData['primjedbe'] ??\n+                    sectionData['pdf9PrimjedbeINedostatci'] ??\n+                    sectionData['primjedbeINedostatci'] ??\n+                    sectionData['primedbe'] ??\n+                    '';\n+            section.napomenaController.text =\n+                sectionData['napomena'] ??\n+                    sectionData['pdf9Napomena'] ??\n+                    sectionData['Napomena'] ??\n+                    '';\n```",
    "takeaway": "**Data upgrades must be backwards compatible.** A software update should never destroy a user's legacy local data. This bug proves the absolute necessity of State Payload Testing and verifying that data structures survive version migrations safely."
  },
  {
    "id": "06",
    "title": "WYSIWYG PDF Image Layout Parity",
    "hash": "8b98fa47851bd30388a6058680389231f970f67e",
    "category": "Visual Regression / WYSIWYG (What You See Is What You Get)",
    "severity": "🟡 Medium (Visual Defect / Customer Dissatisfaction)",
    "problem": "Users reported that when they meticulously aligned images within the application's visual editor (specifically in the PDF6 module), the final generated PDF did not match their design. The images were scaling down and shifting out of their configured bounding boxes.",
    "rca": "Visual layout parity bugs between a frontend UI and a backend PDF generator are notoriously common. \nIn this case, the PDF generation logic was ignoring the `useManualLayout` flag correctly. Instead of respecting the user's manual X/Y coordinates and forcing the image to paint exactly in the defined bounding box, the generator was continuously applying an auto-scaling algorithm (`math.min(scaleX, scaleY)`) that overrode the user's manual layout.",
    "codeProof": "I split the drawing logic. If `useManualLayout` is true, the generator now completely bypasses the auto-scaling logic and draws the bitmap *exactly* at the configured `targetBox` coordinates, establishing 1:1 visual parity.\n\n```diff\n--- a/lib/screens/pregledpdf.dart\n+++ b/lib/screens/pregledpdf.dart\n@@ -3176,34 +3176,44 @@ class _PregledPdfPageState extends State<PregledPdfPage>\n \n               if (image.useManualLayout) {\n                 targetBoxX = cellStartX + (image.manualX * cellWidth);\n-                targetBoxY = imageStartY + (image.manualY * cellHeight);\n+                targetBoxY = pdf6CurrentY + (image.manualY * cellHeight);\n                 targetBoxWidth = image.manualWidth * cellWidth;\n                 targetBoxHeight = image.manualHeight * cellHeight;\n               }\n \n-              final double scaleX = targetBoxWidth / decoded.width;\n-              final double scaleY = targetBoxHeight / decoded.height;\n-              final double fitScale = math.min(scaleX, scaleY);\n-              final double drawWidth = decoded.width * fitScale;\n-              final double drawHeight = decoded.height * fitScale;\n-\n-              final double dx =\n-                  targetBoxX + (targetBoxWidth - drawWidth) / 2;\n-              final double dy =\n-                  targetBoxY + (targetBoxHeight - drawHeight) / 2;\n-\n-              if (dy + drawHeight > cellEndY) {\n-                if (!image.useManualLayout) {\n+              final PdfBitmap bitmap = PdfBitmap(image.bytes);\n+              if (image.useManualLayout) {\n+                // Match editor behavior: draw exactly in configured box.\n+                pdf6Page.graphics.drawImage(\n+                  bitmap,\n+                  Rect.fromLTWH(\n+                    targetBoxX,\n+                    targetBoxY,\n+                    targetBoxWidth,\n+                    targetBoxHeight,\n+                  ),\n+                );\n+              } else {\n+                  // Auto-scale logic omitted for brevity...\n```",
    "takeaway": "**Visual Parity requires strict 1:1 validation.** When testing WYSIWYG editors, QA cannot just verify that \"an image was generated.\" We must verify that the geometric coordinates mapped by the user directly translate to the exported artifact."
  },
  {
    "id": "07",
    "title": "Data Integrity & API Memory Uploads",
    "hash": "7da2cef77885105a700bfb048434d949f808366b",
    "category": "API Integration / Data Mapping",
    "severity": "🛑 High (Cloud Upload Failure & Incorrect Document Data)",
    "problem": "Two distinct but related bugs were occurring in the Zaštita od požara (ZOP) and ZNR modules:\n1. The cloud backup upload to Backblaze B2 was failing, returning an error that the PDF file could not be found locally.\n2. The dates printed on the generated certificates (\"Potvrde\") were incorrect—they were pulling the general report date instead of the specific training execution date.",
    "rca": "1. **Upload Failure:** The app was attempting to save the PDF to a hardcoded macOS disk path (`/Users/...`) before immediately reading it back from the disk to upload to the B2 cloud. On Windows, the write failed, meaning the subsequent read/upload also failed.\n2. **Data Mapping:** The UI form was improperly mapping the generic `_datumController` to the certificate logic, entirely ignoring the `_datumIzvrsenjaObukeController` (Date of Training Execution) that the user had explicitly filled out.",
    "codeProof": "I resolved the upload bug by bypassing local disk I/O entirely. The generated PDF is now kept in a memory buffer (`Uint8List`) and streamed directly to the B2 API. \nI also fixed the data mapping to ensure the correct Date Controller is routed to the certificate generator.\n\n```diff\n--- a/lib/screens/zastita_od_pozara_screen.dart\n+++ b/lib/screens/zastita_od_pozara_screen.dart\n@@ -1135,19 +1135,12 @@ class _ZastitaOdPozaraScreenState extends State<ZastitaOdPozaraScreen> {\n-      final String folderPath =\n-          '/Users/emirnurkovic/Desktop/urnecitest';\n-      final Directory outputDir = Directory(folderPath);\n-      if (!outputDir.existsSync()) {\n-        outputDir.createSync(recursive: true);\n-      }\n-\n       final String rawName = _brojObukeController.text.trim();\n       final String safeName =\n           rawName.isEmpty ? 'zop' : rawName.replaceAll(RegExp(r'[^\\w\\-]+'), '_');\n       final String fileName = '$safeName.pdf';\n-      final File outputFile = File('${outputDir.path}/$fileName');\n-      await outputFile.writeAsBytes(Uint8List.fromList(await document.save()));\n+      final Uint8List pdfBytes =\n+          Uint8List.fromList(await document.save());\n       document.dispose();\n \n@@ -1170,7 +1163,7 @@ class _ZastitaOdPozaraScreenState extends State<ZastitaOdPozaraScreen> {\n             await b2Service.uploadFile(\n               'ASINSPEKT',\n               pdfFileName,\n-              await outputFile.readAsBytes(),\n+              pdfBytes,\n```\n\n```diff\n--- a/lib/screens/zastita_na_radu_screen.dart\n+++ b/lib/screens/zastita_na_radu_screen.dart\n@@ -1507,7 +1507,8 @@ class _ZastitaNaRaduScreenState extends State<ZastitaNaRaduScreen> {\n-        final String provjeraDatum = _datumController.text.trim();\n+        final String provjeraDatum =\n+            _datumIzvrsenjaObukeController.text.trim();\n         potvrdaPage.graphics.drawString(\n           'Sarajevo. Sarajevo, $provjeraDatum. godine',\n```",
    "takeaway": "**Memory buffers are safer than Disk I/O.** When testing API integrations, QA should challenge the architecture: *Why are we writing to disk just to read it back a millisecond later?* Bypassing disk operations reduces points of failure and entirely eliminates cross-platform pathing bugs."
  },
  {
    "id": "08",
    "title": "OS-Level Script Encoding Failures",
    "hash": "38a5baa5d4a2484e414cee0967cbde2312e655f8",
    "category": "Dev Environment / Character Encoding",
    "severity": "🔥 Critical (Auto-Updater Broken on Windows)",
    "problem": "The application includes a self-updating mechanism that downloads a new `.msix` release and executes a PowerShell script to install it. On Windows machines, the script was failing silently. The app would download the update, but the installation would never trigger.",
    "rca": "This was a deep OS-level character encoding issue. \nThe Dart code was dynamically generating the PowerShell script (`updater.ps1`) using Unix-style line endings (`\\n`) and standard UTF-8 encoding. However, older or strictly-configured Windows PowerShell environments reject or misinterpret scripts that do not use Windows Carriage-Return Line-Feeds (`\\r\\n`) or specific ASCII/UTF-16 encoding. \nAdditionally, the script execution was being blocked by default Windows Execution Policies.",
    "codeProof": "I injected an explicit replace function to force `\\r\\n` line endings, strictly set the file encoding to `ascii`, and appended `-NoProfile` and `-NoLogo` to the PowerShell execution arguments to ensure a clean, headless shell launch.\n\n```diff\n--- a/lib/services/update_service.dart\n+++ b/lib/services/update_service.dart\n@@ -218,11 +218,18 @@ exit 0\n ''';\n \n     try {\n-      await updaterScript.writeAsString(scriptContents, flush: true);\n+      final scriptWithCrlf = scriptContents.replaceAll('\\n', '\\r\\n');\n+      await updaterScript.writeAsString(\n+        scriptWithCrlf,\n+        flush: true,\n+        encoding: ascii,\n+      );\n       final updatedVersionUrl = _applyTokenToUri(versionJsonUrl).toString();\n       await Process.start(\n         'powershell',\n         [\n+          '-NoProfile',\n+          '-NoLogo',\n           '-ExecutionPolicy',\n           'Bypass',\n           '-File',\n```",
    "takeaway": "**Test the Delivery Mechanism, not just the App.** An application is useless if the user cannot install it. This bug emphasizes the importance of testing deployment and upgrade paths natively on the target operating system, specifically hunting for encoding and permission boundaries."
  },
  {
    "id": "09",
    "title": "Draft Autosave Resilience",
    "hash": "291887ec7e8b4e2f4f107f9b4c0bb8a7e0f2f3d9",
    "category": "Data Retention / Resilience",
    "severity": "🔥 Critical (Data Loss)",
    "problem": "QA Exploratory Testing revealed a massive data-loss risk. If a user spent 45 minutes filling out a massive ZOP (Zaštita od požara) form and the application crashed, their computer lost power, or they accidentally closed the window, 100% of their form state was lost.",
    "rca": "The application architecture held the entire form state exclusively in volatile RAM. There was no background worker saving the session to the disk, meaning any interruption to the application lifecycle resulted in a total wipe of the user's data.",
    "codeProof": "I implemented a `DraftAutosaveService` that writes the application state to the OS's `getApplicationSupportDirectory()`. Crucially, I hooked the trigger to the top-level `Form.onChanged` event, and added a specific deletion mechanism (`clearDraft`) that wipes the draft *only* after a successful PDF generation.\n\n```diff\n--- a/lib/services/draft_autosave_service.dart\n+++ b/lib/services/draft_autosave_service.dart\n@@ -0,0 +1,43 @@\n+class DraftAutosaveService {\n+  static Future<File> _draftFile(String draftKey) async {\n+    final Directory dir = await getApplicationSupportDirectory();\n+    return File('${dir.path}${Platform.pathSeparator}drafts$draftKey.json');\n+  }\n+\n+  static Future<void> saveDraft(String draftKey, Map<String, dynamic> data) async {\n+    final File file = await _draftFile(draftKey);\n+    await file.writeAsString(jsonEncode(data), flush: true);\n+  }\n```\n\n```diff\n--- a/lib/screens/zastita_od_pozara_screen.dart\n+++ b/lib/screens/zastita_od_pozara_screen.dart\n@@ -2002,6 +2138,7 @@ class _ZastitaOdPozaraScreenState extends State<ZastitaOdPozaraScreen> {\n           padding: const EdgeInsets.all(24),\n           child: Form(\n             key: _formKey,\n+            onChanged: _scheduleAutosave,\n             child: isWideLayout\n```\n\n```diff\n--- a/lib/screens/zastita_od_pozara_screen.dart\n+++ b/lib/screens/zastita_od_pozara_screen.dart\n@@ -1191,6 +1299,8 @@ class _ZastitaOdPozaraScreenState extends State<ZastitaOdPozaraScreen> {\n       }\n \n       if (!mounted) return;\n+      await DraftAutosaveService.clearDraft(_draftKey);\n```",
    "takeaway": "**Destructive testing is mandatory.** A QA Engineer must actively try to break the app by force-quitting it mid-session, simulating battery death, or dropping network connections. Testing the \"happy path\" is insufficient for enterprise data-entry tools."
  },
  {
    "id": "10",
    "title": "CI/CD Build Idempotency",
    "hash": "6ed9b59417a16760299feac70f103c5fc611adcf",
    "category": "DevOps / Pipeline Integrity",
    "severity": "🟡 Medium (Flaky Builds / Bloated Artifacts)",
    "problem": "The GitHub Actions workflow that generated the Windows release (`.exe` / `.msix`) was behaving unpredictably. Some builds failed randomly, and the artifacts produced were massive or contained strange temporary files that shouldn't exist in a production release.",
    "rca": "Two issues were polluting the pipeline:\n1. **Ephemeral Bleed:** The `windows/flutter/ephemeral/` directory was not explicitly ignored in `.gitignore`, meaning temporary local build files could bleed into the repository and corrupt the CI runner's clean state.\n2. **Lack of Idempotency:** The GitHub Actions runner was using a cached environment to speed up builds but was *not* cleaning old artifacts before compiling. This led to stale binaries getting zipped up with the new release.",
    "codeProof": "I fortified the pipeline by enforcing strict idempotency. I added `flutter clean` before every build to nuke the cached artifact tree, explicitly ignored the `ephemeral` folder in `.gitignore`, and used `Compress-Archive` to package only the clean `Release/*` binaries.\n\n```diff\n--- a/.gitignore\n+++ b/.gitignore\n@@ -32,6 +32,7 @@ migrate_working_dir/\n .pub/\n /build/\n /coverage/\n+windows/flutter/ephemeral/\n```\n\n```diff\n--- a/.github/workflows/windows-build.yml\n+++ b/.github/workflows/windows-build.yml\n@@ -28,14 +29,19 @@ jobs:\n       - name: Install dependencies\n         run: flutter pub get\n \n+      - name: Clean old build artifacts\n+        run: flutter clean\n+\n       - name: Build Windows\n         run: flutter build windows --release\n \n+      - name: Package release folder\n+        run: |\n+          Compress-Archive -Path \"build/windows/x64/runner/Release/*\" -DestinationPath \"as_inspekt-windows-release.zip\"\n```",
    "takeaway": "**Pipelines must be Idempotent.** A CI/CD environment must produce the exact same binary output every single time given the same source code. Relying on unclean, cached environments is a massive QA vulnerability that allows \"Works on my machine\" bugs to leak into production releases."
  },
  {
    "id": "11",
    "title": "UI Table Scrolling Overflow",
    "hash": "0569c14",
    "category": "UI/UX Rendering Constraints",
    "severity": "🟡 Medium (Inaccessible UI Elements)",
    "problem": "During manual UI testing, I populated the ZNR/ZOP employee table with 20 mock records. However, the internal layout engine failed to constrain the list. The table overflowed the screen boundaries, pushing the \"Save\" and \"Submit\" buttons off-screen and rendering them entirely unclickable.",
    "rca": "Flutter requires explicit height constraints (like a `SizedBox`, `Expanded`, or `Flexible`) when nesting scrollable elements (`ListView`) inside other scrollable parents or columns. Because the table's `ListView` was unbound, it attempted to take infinite height, breaking the render tree constraints.",
    "codeProof": "I enclosed the table within an explicit `SizedBox` bound by `itemExtent`, added a `Scrollbar` controller for accessibility, and properly constrained the UI so that no matter how many rows were added, the layout remained static and scrollable.\n\n```diff\n--- a/lib/screens/zastita_na_radu_screen.dart\n+++ b/lib/screens/zastita_na_radu_screen.dart\n@@ -62,0 +63,22 @@\n+          const SizedBox(height: 8),\n+          SizedBox(\n+            height: _osobaRowExtent * _fixedVisibleRows,\n+            child: Scrollbar(\n+              controller: _radniciScrollController,\n+              thumbVisibility: true,\n+              child: ListView.builder(\n+                controller: _radniciScrollController,\n+                padding: EdgeInsets.zero,\n+                itemExtent: _osobaRowExtent,\n```",
    "takeaway": "**Data Volume Testing is critical for UI.** A developer might test a table with 2 rows. A QA engineer tests it with 0 rows, 1 row, and 100 rows. This bug highlights the importance of volume testing to uncover bounding-box constraint failures in the UI."
  },
  {
    "id": "12",
    "title": "PDF Text Wrapping Engine",
    "hash": "6414f9611c220285d54f49b6c7ac97f2f41cc9b9",
    "category": "Document Generation / PDF Bounds",
    "severity": "🛑 High (Generated Report Invalid)",
    "problem": "In the final generated PDF reports (specifically PDF11), if the \"Objekat\" field contained a very long string, the text would render in a single horizontal line, bleeding completely off the right edge of the A4 page and getting cut off permanently.",
    "rca": "The native `Syncfusion` PDF library drawing command `graphics.drawString` was given static bounding boxes and lacked an automated line-break mechanism for single-string payloads in that specific method overload.",
    "codeProof": "I wrote a custom text-wrapping algorithm (`wrapWithMaxLines`) that dynamically measures the pixel width of the font (`font.measureString`), splits the text block safely at spaces, and constructs a multi-line array that strictly obeys the defined `maxWidth` of the PDF bounding box.\n\n```diff\n--- a/lib/screens/pregledpdf.dart\n+++ b/lib/screens/pregledpdf.dart\n@@ -4664,17 +4664,63 @@ class _PregledPdfPageState extends State<PregledPdfPage> {\n-            pdf11Page.graphics.drawString(\n-              objekatValue,\n-              timesRegular,\n-              brush: brush,\n-              bounds: Rect.fromLTWH(\n-                280 * pdf11Scale + pdf11OffsetX,\n-                (470 + pdf11ContentOffsetY) * pdf11Scale,\n-                300,\n-                20,\n-              ),\n-            );\n+            List<String> wrapWithMaxLines(\n+              String text,\n+              PdfFont font,\n+              double maxWidth,\n+              int maxLines,\n+            ) {\n+              final List<String> lines = [];\n+              // Loop and measure width logic...\n+              for (final word in words) {\n+                final String candidate = currentLine.isEmpty ? word : '$currentLine $word';\n+                if (font.measureString(candidate).width <= maxWidth) {\n+                  currentLine = candidate;\n+                } else {\n+                  lines.add(currentLine);\n+                  currentLine = word;\n+                }\n+              }\n```",
    "takeaway": "**Generative Artifacts require maximum boundary testing.** Automated testing can verify the PDF *exists*, but it takes manual boundary testing (inserting absurdly long strings into inputs) to verify the internal PDF engine correctly respects visual constraints on the canvas."
  },
  {
    "id": "13",
    "title": "PDF Template Orientation Bounds",
    "hash": "1dc88a3",
    "category": "PDF Architecture / Layout Disruption",
    "severity": "🟡 Medium (Formatting Defect)",
    "problem": "When appending new pages (tlocrti/floorplans) to an existing PDF template, the layout scaled incorrectly. Floorplans that were in Landscape mode were being forcibly compressed into Portrait mode bounding boxes, resulting in squished, distorted images on the final report.",
    "rca": "The codebase was statically calling `document.pages.add()` and relying on the default framework page size (`page.getClientSize()`). It completely failed to inherit the properties, dimensions, and orientations of the `baseTemplate` it was actually drawing on.",
    "codeProof": "I refactored the page-appending logic to dynamically inspect the `baseTemplatePageSize`. I wrote an `appendPage` helper function that dynamically analyzes if width is greater than height, and explicitly forces `PdfPageOrientation.landscape` or `portrait` at generation time.\n\n```diff\n--- a/lib/screens/zastita_od_pozara_screen.dart\n+++ b/lib/screens/zastita_od_pozara_screen.dart\n@@ -182,11 +266,25 @@ class _ZastitaOdPozaraScreenState extends State<ZastitaOdPozaraScreen> {\n       final PdfDocument document = PdfDocument(inputBytes: templateBytes);\n       final PdfDocument templateDoc = PdfDocument(inputBytes: templateBytes);\n       PdfTemplate? baseTemplate;\n+      Size? baseTemplatePageSize;\n       if (templateDoc.pages.count > 0) {\n         baseTemplate = templateDoc.pages[0].createTemplate();\n+        baseTemplatePageSize = templateDoc.pages[0].size;\n+      }\n+      final PdfMargins zeroMargins = PdfMargins()..all = 0;\n+      PdfPage appendPage(Size pageSize) {\n+        return document.pages.insert(\n+          document.pages.count,\n+          pageSize,\n+          zeroMargins,\n+          PdfPageRotateAngle.rotateAngle0,\n+          pageSize.width > pageSize.height\n+              ? PdfPageOrientation.landscape\n+              : PdfPageOrientation.portrait,\n+        );\n       }\n-      final PdfPage page = document.pages[0];\n-      final Size pageSize = page.getClientSize();\n+      final Size pageSize = baseTemplatePageSize ?? page.getClientSize();\n```",
    "takeaway": "**Test combinations, not just features.** The bug only appeared when a Landscape Floorplan was combined with a Portrait base template. QA matrices must cover state combinations (Portrait + Portrait, Portrait + Landscape) to uncover deep architectural integration flaws."
  },
  {
    "id": "14",
    "title": "UI Dropdown Overflow Failures",
    "hash": "ecf947f29420e6120a7401e4925739df964d67b9",
    "category": "UI Resilience",
    "severity": "🟡 Medium (Visual Blowout)",
    "problem": "In the \"Teoretski izvodjac\" (Theoretical Instructor) dropdown menu, whenever a particularly long name and title was selected (e.g., \"Srđan Drašković, Diplomirani inženjer elektrotehnike\"), the UI would throw a massive red \"Right Overflowed by 42 Pixels\" error, breaking the screen layout.",
    "rca": "Dropdown menus in Flutter attempt to size themselves based on their widest child. Because the parent container had a fixed width, the long string simply broke the layout constraints instead of gracefully truncating.",
    "codeProof": "I enforced the `isExpanded: true` property on the dropdown, forcing it to respect the parent's width. Furthermore, I explicitly added `TextOverflow.ellipsis` and `softWrap: false` to every item in the menu so the text degrades gracefully (...) instead of breaking the render tree.\n\n```diff\n--- a/lib/screens/zastita_od_pozara_screen.dart\n+++ b/lib/screens/zastita_od_pozara_screen.dart\n@@ -1724,6 +1724,7 @@ class _ZastitaOdPozaraScreenState extends State<ZastitaOdPozaraScreen> {\n         ),\n         const SizedBox(height: 8),\n         DropdownButtonFormField<String>(\n+          isExpanded: true,\n           decoration: const InputDecoration(\n             labelText: 'Teoretski izvodjac',\n@@ -1731,15 +1732,27 @@ class _ZastitaOdPozaraScreenState extends State<ZastitaOdPozaraScreen> {\n           items: const [\n             DropdownMenuItem(\n               value: 'Srđan Drašković, Diplomirani inženjer elektrotehnike',\n-              child: Text('Srđan Drašković, Diplomirani inženjer elektrotehnike'),\n+              child: Text(\n+                'Srđan Drašković, Diplomirani inženjer elektrotehnike',\n+                overflow: TextOverflow.ellipsis,\n+                softWrap: false,\n+              ),\n             ),\n```",
    "takeaway": "**Defensive Design is a Quality Metric.** Good QA catches bad design. Inputs shouldn't just *handle* long strings; the UI must actively defend itself against them. Every dynamic text widget must have explicit overflow behaviors defined and tested."
  },
  {
    "id": "15",
    "title": "Removing Heavy Dependencies",
    "hash": "0353542",
    "category": "Dependency Management / CI Bloat",
    "severity": "🛑 High (Pipeline Failure Risk / Technical Debt)",
    "problem": "The auto-updater system was originally written as a standalone C# (.NET 6.0) executable (`ASInspektUpdater.csproj`) that was compiled and shipped with the Flutter app. \nDuring QA pipeline reviews, I found that this caused massive CI/CD bottlenecks. The pipeline had to install the full .NET SDK just to compile a 200-line updater script, leading to bloated artifacts, frequent timeout failures on Windows runners, and severe technical debt.",
    "rca": "Compiling a separate `.exe` for a simple file-replacement task was absolute overkill. It introduced a massive external dependency (.NET runtime) that wasn't native to the primary stack (Flutter/Dart). This drastically increased the surface area for deployment bugs.",
    "codeProof": "I executed a brutal but necessary refactor. I entirely deleted the C# project (`ASInspektUpdater.csproj` and `Program.cs`), removed the `.NET` step from the GitHub Actions workflow, and replaced the entire system with a native, 50-line PowerShell script (`updater.ps1`) generated dynamically by Dart.\n\n```diff\n--- a/.github/workflows/windows-build.yml\n+++ b/.github/workflows/windows-build.yml\n@@ -20,10 +20,6 @@ jobs:\n           channel: stable\n           cache: true\n \n-      - name: Set up .NET\n-        uses: actions/setup-dotnet@v3\n-        with:\n-          dotnet-version: '6.0.x'\n```\n\n```diff\n--- a/updater/ASInspektUpdater/ASInspektUpdater.csproj\n+++ /dev/null\n@@ -1,12 +0,0 @@\n-<Project Sdk=\"Microsoft.NET.Sdk\">\n-  <PropertyGroup>\n-    <OutputType>Exe</OutputType>\n-    <TargetFramework>net6.0</TargetFramework>\n```",
    "takeaway": "**Less Code = Less Bugs.** A great QA Engineer doesn't just test the code that exists; they question if the code should exist at all. By removing a heavy external dependency and relying on native OS tools, I permanently eliminated an entire category of build failures and CI/CD timeout risks."
  },
  {
    "id": "16",
    "title": "PDF Multiline Bounding Failures",
    "hash": "b187a3240997295b217bdf5f89c2bd75593001ec",
    "category": "Visual Regression / Rendering",
    "severity": "🟡 Medium (Document Formatting Defect)",
    "problem": "During QA validation of the PDF generation module, we identified that when combining a company name, address, and city location into a single display block, long inputs (such as a 3-line address) caused text to overflow its bounding box and bleed into neighboring legal text on the PDF.",
    "rca": "The original code appended multiple strings into a list (`companyLines.add()`) but did not measure the pixel width of those strings against the PDF's target `Rect.fromLTWH` bounding box. The text engine blindly printed the characters until they walked off the page.",
    "codeProof": "I wrote a highly defensive `wrapWithMaxLines` algorithm. It measures every word (`font.measureString(candidate).width`) dynamically, calculates when the string will exceed the `maxWidth`, and explicitly calculates character-level splitting for excessively long un-spaced words, protecting the PDF formatting.\n\n```diff\n--- a/lib/screens/pregledpdf.dart\n+++ b/lib/screens/pregledpdf.dart\n@@ -2277,23 +2277,108 @@ class _PregledPdfPageState extends State<PregledPdfPage> {\n+        List<String> wrapWithMaxLines(\n+          String text,\n+          PdfFont font,\n+          double maxWidth,\n+          int maxLines,\n+        ) {\n+          // Evaluates font.measureString(word).width and strictly forces maxLines array constraints...\n+        }\n+\n+        final List<String> companyLines = [];\n+        final String primaryLine = adresaFirmeValue.isNotEmpty\n+            ? '$firmaName, $adresaFirmeValue'\n+            : firmaName;\n+        companyLines.addAll(\n+          wrapWithMaxLines(primaryLine, timesNewRoman11, 270, 3),\n+        );\n```",
    "takeaway": "**Never trust dynamic strings in fixed bounds.** When automating or manual-testing PDF generators, QA must use randomly generated long-text payloads (Lorem Ipsum) to simulate the worst-case boundary limits of visual containers."
  },
  {
    "id": "17",
    "title": "UI State & Grammar Injection",
    "hash": "6095805500c62be8a69fe681db19a0125926afb6",
    "category": "Data Injection / State Sync",
    "severity": "🟡 Medium (Incorrect Output Data)",
    "problem": "QA identified two distinct data rendering flaws in the generated PDF outputs:\n1. In the ZNR module, a sentence read grammatically incorrectly when an address was passed vs a site location (e.g., \"...na adresi gradilištu tunela Kvanj\").\n2. In the ZOP module, the trainer's name was statically hardcoded as \"Nihad Omerhodžić\", completely ignoring if the user had selected a different trainer from the dropdown state!",
    "rca": "1. **Grammar bug:** The UI prompt wasn't clear to the user, leading them to type full prepositions, which clashed with hardcoded prepositions in the PDF engine.\n2. **State Sync bug:** The PDF generator was entirely ignoring the `_teoretskiIzvodjac` variable, favoring a hardcoded fallback string.",
    "codeProof": "I updated the UI hint to clarify input formatting and removed the hardcoded preposition in the PDF generator. More importantly, I injected the correct state variable (`teoretskiIzvodjacText`) into the ZOP graphics engine, ensuring dynamic data sync.\n\n```diff\n--- a/lib/screens/zastita_na_radu_screen.dart\n+++ b/lib/screens/zastita_na_radu_screen.dart\n@@ -1240,7 +1240,7 @@ class _ZastitaNaRaduScreenState extends State<ZastitaNaRaduScreen> {\n         _TextSegment(\n           text: _lokacijaPrefix.trim() == 'u'\n               ? ' godine u skladu sa odredbama Zakona o zaštiti na radu... u '\n-              : ' godine u skladu sa odredbama Zakona o zaštiti na radu... na adresi ',\n+              : ' godine u skladu sa odredbama Zakona o zaštiti na radu... na ',\n           font: bodyFont,\n```\n\n```diff\n--- a/lib/screens/zastita_od_pozara_screen.dart\n+++ b/lib/screens/zastita_od_pozara_screen.dart\n@@ -1067,8 +1069,10 @@ class _ZastitaOdPozaraScreenState extends State<ZastitaOdPozaraScreen> {\n+        final String teoretskiIzvodjacText =\n+            _teoretskiIzvodjac ?? 'Nihad Omerhodžić, Ing. Sigurnosti i pomoći';\n         page4.graphics.drawString(\n-          '1. Nihad Omerhodžić, Ing. Sigurnosti i pomoći',\n+          '1. $teoretskiIzvodjacText',\n```",
    "takeaway": "**Hardcoded fallbacks destroy state integrity.** QA testing must explicitly include switching default values in dropdowns and verifying that the final generated artifacts reflect those state changes rather than defaulting to fallback values."
  },
  {
    "id": "18",
    "title": "Array Reordering State Logic",
    "hash": "98df18515cab60bdd6dfabc5244025e795b958e7",
    "category": "State Management / Array Logic",
    "severity": "🛑 High (State Desynchronization)",
    "problem": "Users needed the ability to reorder complex, nested data sections (Predmeti) in the UI. However, early manual testing revealed that moving an item up or down in the list caused the currently selected item (the one active in the right-hand editor pane) to desynchronize, load the wrong data, or throw an `IndexOutOfBounds` exception.",
    "rca": "When modifying the master array (`_pdf6Sections`), the active pointer `_selectedPdf6Index` was not being recalculated. If you moved Item 3 to Index 1, the `_selectedPdf6Index` would still point to Index 3, causing the UI to instantly load a completely different data object into the editor.",
    "codeProof": "I wrote a robust array-mutation algorithm (`_movePdf6Section`) that intercepts the boundary limits, safely removes and inserts the object in the array, and then mathematically recalculates the `_selectedPdf6Index` pointer relative to the `fromIndex` and `toIndex` vectors to keep the UI perfectly synchronized.\n\n```diff\n--- a/lib/screens/pregledpdf.dart\n+++ b/lib/screens/pregledpdf.dart\n@@ -658,6 +658,34 @@ class _PregledPdfPageState extends State<PregledPdfPage> {\n+  void _movePdf6Section(int fromIndex, int toIndex) {\n+    if (fromIndex == toIndex) return;\n+    if (fromIndex < 0 || fromIndex >= _pdf6Sections.length || toIndex < 0 || toIndex >= _pdf6Sections.length) {\n+      return; // Array bounds safety check\n+    }\n+    setState(() {\n+      final section = _pdf6Sections.removeAt(fromIndex);\n+      _pdf6Sections.insert(toIndex, section);\n+\n+      final selected = _selectedPdf6Index;\n+      if (selected == null) return;\n+      \n+      // Recalculate pointer to maintain state sync\n+      if (selected == fromIndex) {\n+        _selectedPdf6Index = toIndex;\n+      } else if (fromIndex < selected && toIndex >= selected) {\n+        _selectedPdf6Index = selected - 1;\n+      } else if (fromIndex > selected && toIndex <= selected) {\n+        _selectedPdf6Index = selected + 1;\n+      }\n+    });\n+  }\n```",
    "takeaway": "**Array mutation destroys pointers.** Any UI feature that allows \"drag to reorder\" or \"move up/down\" requires extensive testing of the active state pointer. QA must verify that the item they are editing remains the item they are editing even while the array shifts beneath it."
  }
];
